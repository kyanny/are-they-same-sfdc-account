{
  "name": "are-they-same-sfdc-account",
  "version": "1.0.0",
  "description": "SFDC Account Checker - Check if Salesforce account IDs are the same",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
